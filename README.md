# Netflix Movies Project

The analysis notebook is [`netflix_movies_analysis.ipynb`](netflix_movies_analysis.ipynb).

Start Jupyter from this repository's root directory and run the notebook from top to bottom. It uses only Python's standard library, so no extra packages are required.
